{% trans_default_domain 'FOSUserBundle' %}

<div class="row">
  <form class="form-horizontal" action="{{ path("fos_user_security_check") }}" method="post">
      {% if csrf_token %}
          <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
      {% endif %}

      <div class="form-group">
        <label class="col-sm-3 control-label" for="username">Login :{#{ 'security.login.username'|trans }#}</label>
        <div class="col-sm-7">
          <input type="text" id="username" class="form-control" name="_username" value="{{ last_username }}" required="required" />
        </div>
      </div>

      <div class="form-group">
        <label class="col-sm-3 control-label" for="password">Mot de passe :{#{ 'security.login.password'|trans }#}</label>
        <div class="col-sm-7">
          <input type="password" id="password" class="form-control" name="_password" required="required" />
        </div>
      </div>

      <div class="form-group">
        <div class="col-sm-offset-3 col-sm-8">
          <div class="checkbox">
            <label>
              <input type="checkbox" id="remember_me" name="_remember_me" value="on" /> Se souvenir
            </label>
          </div>
        </div>
      </div>

      <div class="form-group">
        <div class="col-sm-offset-3 col-sm-9">
          <input type="submit" id="_submit" class="btn btn-primary" name="_submit" value="Valider{#{ 'security.login.submit'|trans }#}" />
          <a class="btn btn-default" href="/register">Créer un compte</a>
        </div>
      </div>

  </form>
</div>

<div class="row">
  {% if error %}
    <div class="alert alert-danger alert-dismissible col-sm-offset-1 col-sm-10" role="alert">
      <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
      {{ error.messageKey|trans(error.messageData, 'security') }}
    </div>
  {% endif %}
</div>
