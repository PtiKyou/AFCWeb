{# src/OC/PlatformBundle/Resources/views/Default/index.html.twig #}

{% extends "AFCRunningPlatformBundle::layout.html.twig" %}

{% block title %}
AFCRunning - {{name.nom}}
{%endblock title %}

{% block name %}
  <div class="row">
   <span class="text-info">
   {% for names in name %}
       <h1> {{name.nom}} </h1>
  {% endfor %}
   </span>
   <hr/>
 </div>
{% endblock name %}

{% block container %}
  <div class="row">
      <table class="table">
          <thead>
          <tr>
              <th scope="col"></th>
              <th scope="col"></th>
              <th scope="col"></th>
          </tr>
          </thead>

          <tbody>
    {% for listMenu in listMenus %}




            <tr>
                <td>{{ listMenu.title }}</td>
                <td> <a href="{{ listMenu.url }}/view">
                        <button type="button" class="btn btn-primary">Voir</button>
                    </a></td>
                <td> <a href="{{ listMenu.url }}/add">
                        <button type="button" class="btn btn-primary">Créer</button>
                    </a></td>
            </tr>



    {% endfor %}
          </tbody>
      </table>

  </div>
{% endblock %}
